node_modules/
dist/
.astro/
.env
.env.local
.DS_Store
*.log
.vercel
.wrangler

# Raw scrape of the legacy Manus site — kept locally as the migration source
# of truth, not committed. See docs/MIGRATION.md.
_source/

# Visual-check output from `npm run shots`.
_shots/

# Raw brand assets awaiting processing. Only the optimised output in public/
# is committed. See brand-drop/README.md.
brand-drop/*
!brand-drop/README.md
